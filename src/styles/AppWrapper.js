import styled from 'styled-components';

export const AppWrapper = styled.div`
  max-width: 1200px;
  margin: 30px auto;
  & .wrapper{
    display: flex;
    flex-wrap: wrap;
  }
  h1{
    text-align: center;
    margin-bottom: 40px;
  }
`